import { z } from "zod";
export const snippetFormSchema = z.object({
  //   title: z
  //     .string()
  //     .min(1, "Title is required")
  //     .max(50, "Title maximum is 50 char"),
  //   filename: z.string().min(1, "Filename is required"),
  //   code: z.string().min(1, "code is required"),
});
